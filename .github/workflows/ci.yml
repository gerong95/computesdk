name: CI

on:
  pull_request:
    branches: [ main ]
    types: [opened, synchronize, reopened]

jobs:
  test:
    runs-on: namespace-profile-default
    env:
      AWS_LAMBDA_ROLE_ARN: ${{ secrets.AWS_LAMBDA_ROLE_ARN }}
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

    steps:
    - name: Check for Changeset Release
      id: skip_check
      run: |
        if [[ "${{ github.head_ref || github.ref_name }}" == "changeset-release/"* ]]; then
          echo "skip=true" >> $GITHUB_OUTPUT
          echo "Skipping tests for Changeset Release PR"
        else
          echo "skip=false" >> $GITHUB_OUTPUT
        fi

    - uses: actions/checkout@v4
      if: steps.skip_check.outputs.skip != 'true'
      with:
        # For pull requests, checkout the PR head ref
        # For pushes, checkout the pushed ref (default behavior)
        ref: ${{ github.event.pull_request.head.sha || github.sha }}
    
    - name: Install pnpm
      if: steps.skip_check.outputs.skip != 'true'
      uses: pnpm/action-setup@v4
    
    - name: Use Node.js
      if: steps.skip_check.outputs.skip != 'true'
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: 'pnpm'
    
    - name: Install dependencies
      if: steps.skip_check.outputs.skip != 'true'
      run: pnpm install --ignore-scripts
    
    - name: Build packages
      if: steps.skip_check.outputs.skip != 'true'
      run: pnpm build

    - name: Benchmark ingest smoke test
      if: steps.skip_check.outputs.skip != 'true'
      run: |
        node --input-type=module -e "const endpoint='https://platform.computesdk.com/api/v1/events'; const now=new Date(); const ended=new Date(now.getTime()+1000); const suiteId='suite_ci_'+Date.now(); const event={event:'benchmark.span',installId:'ci',traceId:'trc_ci_'+Date.now(),spanId:'spn_ci_'+Date.now(),operation:'ci.benchmark.smoke',startedAt:now.toISOString(),endedAt:ended.toISOString(),durationMs:1000,status:'ok',attemptCount:1,attempts:[{provider:'ci',candidateIndex:0,startedAt:now.toISOString(),endedAt:ended.toISOString(),durationMs:1000,status:'ok'}],benchVersion:'ci',runtime:'node',os:'linux',arch:'x64',suiteId,suiteLabel:'ci'}; const res=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({events:[event]})}); if(!res.ok){const body=await res.text().catch(()=>'<no-body>'); throw new Error('Benchmark smoke failed: '+res.status+' '+body);}"  
    
    - name: Run all tests (mock mode)
      if: steps.skip_check.outputs.skip != 'true'
      run: pnpm test
      env:
        # Ensure tests run in mock mode
        E2B_API_KEY: ""
        VERCEL_TOKEN: ""
        VERCEL_TEAM_ID: ""
        VERCEL_PROJECT_ID: ""
        DAYTONA_API_KEY: ""
        MODAL_TOKEN_ID: ""
        MODAL_TOKEN_SECRET: ""
    
    - name: Run type checking
      if: steps.skip_check.outputs.skip != 'true'
      run: pnpm typecheck
    
    - name: Run linting
      if: steps.skip_check.outputs.skip != 'true'
      run: pnpm lint

  provider-integration-tests:
    runs-on: namespace-profile-default
    needs: test  # Only run if basic tests pass
    # Run on all pull requests (but skip for changeset releases)
    if: github.event_name == 'pull_request' && !startsWith(github.head_ref, 'changeset-release/')

    strategy:
      matrix:
        provider: [e2b, vercel, daytona, modal, archil]
      fail-fast: false
    
    steps:
    - uses: actions/checkout@v4
      with:
        # For pull requests, checkout the PR head ref
        # For pushes to main, checkout the pushed ref
        ref: ${{ github.event.pull_request.head.sha || github.sha }}
    
    - name: Install pnpm
      uses: pnpm/action-setup@v4
    
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: 'pnpm'
    
    - name: Install dependencies
      run: pnpm install --ignore-scripts
    
    - name: Build packages
      run: pnpm build
        
    - name: Run E2B Integration Tests
      if: matrix.provider == 'e2b'
      env:
         E2B_API_KEY: ${{ secrets.E2B_API_KEY }}
         COMPUTESDK_API_KEY: ${{ secrets.COMPUTESDK_API_KEY }}
      run: |
         if [ -n "$E2B_API_KEY" ]; then
           echo "Running E2B integration tests with real API..."
           pnpm --filter @computesdk/e2b test
         else
           echo "Skipping E2B integration tests - no API key provided"
         fi
        
    - name: Run Vercel Integration Tests
      if: matrix.provider == 'vercel'
      env:
        VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        VERCEL_TEAM_ID: ${{ secrets.VERCEL_TEAM_ID }}
        VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        COMPUTESDK_API_KEY: ${{ secrets.COMPUTESDK_API_KEY }}
      run: |
         if [ -n "$VERCEL_TOKEN" ] && [ -n "$VERCEL_TEAM_ID" ] && [ -n "$VERCEL_PROJECT_ID" ]; then
           echo "Running Vercel integration tests with real API..."
           pnpm --filter @computesdk/vercel test
         else
           echo "Skipping Vercel integration tests - missing required secrets"
         fi
        
    - name: Run Daytona Integration Tests
      if: matrix.provider == 'daytona'
      env:
        DAYTONA_API_KEY: ${{ secrets.DAYTONA_API_KEY }}
        COMPUTESDK_API_KEY: ${{ secrets.COMPUTESDK_API_KEY }}
      run: |
         if [ -n "$DAYTONA_API_KEY" ]; then
           echo "Running Daytona integration tests with real API..."
           pnpm --filter @computesdk/daytona test
         else
           echo "Skipping Daytona integration tests - no API key provided"
         fi
        
    - name: Run Modal Integration Tests
      if: matrix.provider == 'modal'
      env:
        MODAL_TOKEN_ID: ${{ secrets.MODAL_TOKEN_ID }}
        MODAL_TOKEN_SECRET: ${{ secrets.MODAL_TOKEN_SECRET }}
        COMPUTESDK_API_KEY: ${{ secrets.COMPUTESDK_API_KEY }}
      run: |
         if [ -n "$MODAL_TOKEN_ID" ] && [ -n "$MODAL_TOKEN_SECRET" ]; then
           echo "Running Modal integration tests with real API..."
           pnpm --filter @computesdk/modal test
         else
           echo "Skipping Modal integration tests - missing required secrets"
         fi

    - name: Run Archil Integration Tests
      if: matrix.provider == 'archil'
      env:
        ARCHIL_API_KEY: ${{ secrets.ARCHIL_API_KEY }}
        ARCHIL_REGION: ${{ secrets.ARCHIL_REGION }}
        ARCHIL_DISK_ID: ${{ secrets.ARCHIL_DISK_ID }}
      run: |
         if [ -n "$ARCHIL_API_KEY" ] && [ -n "$ARCHIL_REGION" ] && [ -n "$ARCHIL_DISK_ID" ]; then
           echo "Running Archil integration tests with real API..."
           pnpm --filter @computesdk/archil test
         else
           echo "Skipping Archil integration tests - missing required secrets (ARCHIL_API_KEY, ARCHIL_REGION, ARCHIL_DISK_ID)"
         fi

  computesdk-integration-tests:
    runs-on: namespace-profile-default
    needs: test
    if: github.event_name == 'pull_request' && !startsWith(github.head_ref, 'changeset-release/')

    strategy:
      matrix:
        provider: [e2b, vercel, daytona, modal, archil]
      fail-fast: false

    steps:
    - uses: actions/checkout@v4
      with:
        ref: ${{ github.event.pull_request.head.sha || github.sha }}
    
    - name: Install pnpm
      uses: pnpm/action-setup@v4
    
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: 'pnpm'
    
    - name: Install dependencies
      run: pnpm install --ignore-scripts
    
    - name: Build packages
      run: pnpm build
        
    - name: Run ComputeSDK Integration Test (${{ matrix.provider }})
      env:
        COMPUTESDK_INTEGRATION: "1"
        TEST_PROVIDER: ${{ matrix.provider }}
        E2B_API_KEY: ${{ secrets.E2B_API_KEY }}
        VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        VERCEL_TEAM_ID: ${{ secrets.VERCEL_TEAM_ID }}
        VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        DAYTONA_API_KEY: ${{ secrets.DAYTONA_API_KEY }}
        MODAL_TOKEN_ID: ${{ secrets.MODAL_TOKEN_ID }}
        MODAL_TOKEN_SECRET: ${{ secrets.MODAL_TOKEN_SECRET }}
        ARCHIL_API_KEY: ${{ secrets.ARCHIL_API_KEY }}
        ARCHIL_REGION: ${{ secrets.ARCHIL_REGION }}
        ARCHIL_DISK_ID: ${{ secrets.ARCHIL_DISK_ID }}
      run: |
        case "$TEST_PROVIDER" in
          e2b)
            if [ -z "$E2B_API_KEY" ]; then
              echo "Skipping computesdk integration for e2b - missing E2B_API_KEY"
              exit 0
            fi
            ;;
          vercel)
            if [ -z "$VERCEL_TOKEN" ] || [ -z "$VERCEL_TEAM_ID" ] || [ -z "$VERCEL_PROJECT_ID" ]; then
              echo "Skipping computesdk integration for vercel - missing Vercel credentials"
              exit 0
            fi
            ;;
          daytona)
            if [ -z "$DAYTONA_API_KEY" ]; then
              echo "Skipping computesdk integration for daytona - missing DAYTONA_API_KEY"
              exit 0
            fi
            ;;
          modal)
            if [ -z "$MODAL_TOKEN_ID" ] || [ -z "$MODAL_TOKEN_SECRET" ]; then
              echo "Skipping computesdk integration for modal - missing Modal credentials"
              exit 0
            fi
            ;;
          archil)
            if [ -z "$ARCHIL_API_KEY" ] || [ -z "$ARCHIL_REGION" ] || [ -z "$ARCHIL_DISK_ID" ]; then
              echo "Skipping computesdk integration for archil - missing Archil credentials (ARCHIL_API_KEY, ARCHIL_REGION, ARCHIL_DISK_ID)"
              exit 0
            fi
            ;;
          *)
            echo "Unsupported provider in matrix: $TEST_PROVIDER"
            exit 1
            ;;
        esac

        echo "Running computesdk integration test for $TEST_PROVIDER..."
        pnpm --filter computesdk test compute-provider-integration

  k8s-integration-tests:
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'pull_request' && !startsWith(github.head_ref, 'changeset-release/')
    permissions:
      contents: read

    steps:
    - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      with:
        ref: ${{ github.event.pull_request.head.sha || github.sha }}
        persist-credentials: false

    - name: Install pnpm
      uses: pnpm/action-setup@f40ffcd9367d9f12939873eb1018b921a783ffaa # v4

    - name: Use Node.js
      uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
      with:
        node-version: '22'
        cache: 'pnpm'

    - name: Install dependencies
      run: pnpm install --ignore-scripts

    - name: Set up kind cluster
      uses: helm/kind-action@0025e74a8c7512023d06dc019c617aa3cf561fde # v1.10.0

    - name: Verify cluster is reachable
      run: kubectl get nodes

    - name: Build required packages
      run: pnpm --filter @computesdk/cmd build && pnpm --filter @computesdk/bench build && pnpm --filter daemond build && pnpm --filter computesdk build && pnpm --filter @computesdk/provider build && pnpm --filter @computesdk/test-utils build && pnpm --filter @computesdk/k8s build

    - name: Run Kubernetes provider integration tests
      env:
        COMPUTESDK_TEST_K8S: "1"
      run: pnpm --filter @computesdk/k8s test
